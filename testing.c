#include <stdio.h>
#include <cs50.h>
//Testing git merge

int main(void)
{
	char *string = "AABCDDBBBEA";
	short array[sizeof(string)];
	int size = 0;

	for(int = i; *(string + i) != '\0'; i++)
	{
		switch(*(string + i));
		{
			case 'A':
			array[i] = 1;
			break;

			case 'B':
			array[i] = 2;
			break;

			case 'C'
			array[i] = 3;
			break;

			case 'D'
			array[i] = 4;
			break;

			default
			return 1;
		}

		size++

	}

	int count = 0;
	int tag = 0;
	int curr_loc = 0;
	int curr_longest = 0;

	for(int j = 0; j < size; j++)
	{
		tag = array[i];
		if(count != 0)
		{
			if(array[i] == tag)
			{
				
			}
		}
		else
		{
			count++
		}

		else
		{
			tag = 0;
		}

	}

}
