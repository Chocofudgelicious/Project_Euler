#include <stdio.h>
#include <ctype.h>

int main(void)
{
    float a = 1.01;
    int b = a;

    if(isdigit(float))
    {
        printf("float with decimals is digit.\n");
    }

    float c = a % b;
    printf("after division is: %f.\n", c);
}